scope = "rig"
nudge = "Run gc hook --claim --drain-ack --json, check mail, then work on the claimed task or exit when the action is drain."
idle_timeout = "2h"
min_active_sessions = 1
max_active_sessions = 5
